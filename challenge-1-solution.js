let numberArray = [9, 16, 100, 75, 1000];
function squareRoothOfNumber() {
  for (let i = 0; i < numberArray.length; i++) {
    let squareRoot = Math.sqrt(numberArray[i]);
    console.log(squareRoot);
  }
}squareRoothOfNumber();
